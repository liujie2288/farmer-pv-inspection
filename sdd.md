# 光伏巡检系统概要设计文档

# 1\. 文档概述

## 1\.1 编写目的

本文档基于《光伏巡检系统需求规格说明书》进行编制，核心目的是明确光伏巡检系统的总体架构设计、技术栈选型标准、功能模块划分细则、数据库结构设计、核心业务逻辑实现及接口设计规范，为系统全流程开发工作提供清晰、可落地的技术指导。通过本文档，确保开发团队、测试团队、运维团队形成统一认知，规范开发流程、提升开发效率，严格落实需求文档中所有功能要求、业务约束及交互规范，保障系统开发质量，确保最终交付的系统能够满足光伏巡检场景的实际使用需求。

## 1\.2 适用范围

本概要设计文档适用于光伏巡检系统开发全流程相关参与人员，具体包括系统后端开发人员、前端开发人员、测试工程师、运维工程师及项目管理人员。文档内容覆盖系统开发、测试、部署、维护的全生命周期，作为后端接口开发、前端页面渲染、数据库搭建配置、系统部署运维的核心依据，同时为后续系统功能扩展、版本迭代、故障排查及日常维护提供详细的技术参考，确保系统长期稳定运行。

## 1\.3 设计原则

- 贴合需求：严格遵循《光伏巡检系统需求规格说明书》中的所有功能定义、业务流程约束及用户交互规范，确保设计方案与实际需求高度一致，不偏离核心业务目标，避免无效设计和冗余开发。

- 技术适配：选型成熟、稳定、易维护、可扩展的技术栈，充分考虑光伏巡检场景中移动端优先使用的特点，兼顾移动端适配性与系统运行性能，同时降低开发、测试及运维成本，确保技术方案具备可行性和落地性。

- 架构清晰：采用分层架构模式，明确各层职责边界，实现模块间低耦合、高内聚，减少模块间的依赖关系，便于后续功能扩展、版本迭代及问题定位，提升系统的可维护性和可扩展性。

- 安全可靠：融入全方位的安全设计，包括基于角色的权限控制、用户密码加密存储、数据传输加密、数据防篡改、非法请求拦截等，保障系统访问安全、数据存储安全及业务操作安全，避免数据泄露和非法操作。

- 高效易用：优化核心业务流程，重点简化巡检填报、数据导出、计划管理等高频操作的路径，提升系统运行效率，降低用户操作难度，兼顾管理员与巡检员的使用场景，提升整体用户体验。

## 1\.4 参考文档

《光伏巡检系统需求规格说明书》，作为本概要设计文档的核心参考依据，明确了系统的功能需求、业务约束、用户交互要求及性能指标，是所有设计内容的出发点和落脚点。

# 2\. 系统总体架构

## 2\.1 架构分层设计

为实现系统模块化、可维护、可扩展的设计目标，系统采用前后端分离架构模式，整体自上而下分为6个层次，各层次职责清晰、相互独立、协同工作，通过标准化接口实现数据交互，具体分层及职责如下（从上层到下层依次说明）：

1. 前端展示层：作为用户与系统交互的核心入口，负责页面渲染、用户操作触发、数据展示及交互反馈，优先适配移动端（巡检员户外作业主要使用场景），同时支持PC端响应式适配（管理员后台操作场景），核心实现页面布局展示、用户操作采集、巡检数据可视化及各类反馈提示。

2. API接口层：作为前后端交互的桥梁，负责接收前端发送的HTTP请求，对请求参数进行合法性校验、用户权限拦截及请求格式转换，将校验通过的请求转发至业务逻辑层，同时接收业务逻辑层的处理结果，进行格式封装后返回给前端，确保前后端数据交互的规范、安全。

3. 业务逻辑层：系统的核心业务处理层，负责实现所有业务逻辑规则，接收API接口层转发的请求，协调数据访问层进行数据操作、通用工具层提供的工具服务，完成巡检计划管理、巡检作业填报、数据统计分析等核心业务流程，确保业务规则落地执行。

4. 数据访问层：负责与数据持久层进行交互，封装数据访问逻辑，提供标准化的CRUD（增删改查）操作接口，接收业务逻辑层的指令，执行数据库查询、数据新增、修改、删除等操作，将操作结果返回给业务逻辑层，隔离业务逻辑与数据存储细节。

5. 数据持久层：负责系统所有数据的存储与持久化，分为结构化数据存储和非结构化数据存储两部分，其中MySQL数据库用于存储用户、项目、电站、巡检记录等结构化数据，MinIO/阿里云OSS对象存储用于存储巡检照片、PDF巡检报告等非结构化数据，确保数据的安全存储和高效访问。

6. 通用工具层：提供全局通用的工具类和服务，供其他各层调用，包括图片压缩与水印处理、PDF生成与预览、异步任务处理、数据加密解密、接口参数校验、对象存储操作等，减少代码冗余，提升开发效率，确保系统功能的统一性和规范性。

## 2\.2 技术栈选型

结合系统设计原则、业务需求及光伏巡检场景特点，选型成熟、稳定、适配性强的技术栈，兼顾开发效率、系统性能及维护成本，具体技术栈选型如下：

### 2\.2\.1 前端技术栈

- 核心框架：React 18，作为前端页面开发的核心框架，具备高效的组件化开发能力、虚拟DOM渲染机制，能够快速构建复杂交互界面，同时支持移动端适配，提升页面渲染速度和用户体验。

- 请求工具：Axios，作为前端HTTP请求处理工具，支持请求拦截、响应拦截，能够实现请求参数统一处理、权限令牌携带、异常响应统一处理，适配系统权限校验需求，确保前后端数据交互的稳定可靠。

- 辅助工具：PDF\.js用于实现PDF巡检报告的在线预览功能，js\-cookie用于存储用户登录状态等关键信息，lodash提供丰富的前端工具函数，简化数据处理、数组操作等常用功能的开发。

- 打包工具：Webpack，用于前端资源的打包、压缩、优化，减少前端资源体积，提升页面加载速度，同时支持环境配置区分，适配开发、测试、生产等不同环境需求。

### 2\.2\.2 后端技术栈

- 核心框架：SpringBoot 2\.7\.x，作为后端开发的核心框架，具备快速开发、自动装配、简化配置的特点，能够快速搭建后端服务，减少冗余配置，提升开发效率，同时具备良好的扩展性和兼容性。

- 持久层框架：MyBatis\-Plus，基于MyBatis进行封装优化，提供标准化的CRUD接口、分页查询、条件查询、批量操作等功能，简化数据访问层开发，减少重复代码，提升开发效率，同时支持自定义SQL，满足复杂查询需求。

- 权限框架：Spring Security，基于角色的权限控制框架，能够实现用户登录鉴权、接口权限拦截、角色分配等功能，支持自定义权限规则，确保系统访问安全，适配管理员与巡检员的权限区分需求。

- 辅助工具：
  - EasyExcel，轻量级Excel处理工具，支持Excel文件的导入与导出，适配电站信息批量导入、巡检数据批量导出等场景，处理效率高、占用资源少；
  - iTextPDF，用于PDF巡检报告的生成，支持报告模板定制、内容填充、PDF压缩等功能，满足巡检报告在线预览与导出需求；
  - MinIO/OSS SDK，用于对象存储的操作，实现巡检照片、PDF报告的上传、下载、删除等功能，支持访问权限控制，确保非结构化数据的安全存储；
  - Quartz，定时任务框架，用于处理巡检计划状态自动切换、数据库定时备份、异步导出任务提醒等场景，提升系统自动化运行能力；
  - Lombok，通过注解简化实体类代码编写，自动生成getter、setter、构造方法等，减少代码冗余，提升开发效率。

### 2\.2\.3 数据存储

- 关系型数据库：MySQL 8\.0，作为系统结构化数据的核心存储载体，具备稳定可靠、性能优越、兼容性强的特点，支持事务处理、索引优化等功能，用于存储用户信息、项目信息、电站信息、巡检计划、巡检记录等结构化数据，确保数据的一致性和完整性。

- 对象存储：MinIO/阿里云OSS，用于存储系统中的非结构化数据，包括巡检过程中上传的照片、生成的PDF巡检报告等，具备高可用、高扩展性、访问速度快的特点，支持访问权限控制，能够有效保障非结构化数据的安全存储和高效访问。

### 2\.2\.4 部署环境

- 容器化：Docker，采用容器化部署方式，将应用程序及所有依赖环境封装为Docker容器，实现环境一致性，避免开发环境与生产环境差异导致的问题，同时简化部署流程、提升部署效率，便于系统的扩容和迁移。

- Web服务器：Nginx，作为前端静态资源的部署服务器和后端接口的反向代理服务器，负责前端页面的静态资源加载、请求转发、负载均衡等功能，提升系统访问速度和稳定性，同时具备一定的安全防护能力。

- 服务器：Linux，选用Linux系统作为服务器操作系统，具备稳定可靠、安全高效、资源占用低的特点，适合后端服务、数据库及对象存储的部署，能够保障系统长期稳定运行。

# 2\.3 技术难点与解决方案

结合光伏巡检系统的业务场景（户外移动作业、多端适配、大量非结构化数据处理、数据实时同步）及技术选型，梳理系统开发过程中的核心技术难点，针对性制定可落地的解决方案，确保系统稳定、高效、合规运行，具体如下：

## 2\.3\.1 移动端定位精度不足

难点描述：巡检员户外作业时，受地形（如山区、楼宇遮挡）、天气、设备信号等影响，移动端GPS定位易出现偏差，导致巡检经纬度采集不准确，无法真实反映巡检位置，影响巡检合规性。

解决方案：采用"GPS\+北斗双模定位"提升定位精度，优先选用信号更强的定位模式；添加定位偏差校验机制，当定位精度低于预设阈值（如5米）时，提示巡检员手动微调经纬度，并保留手动调整记录；结合基站辅助定位，在卫星信号较弱场景下，补充基站定位数据，降低定位偏差；定期校准移动端定位权限，确保定位功能正常启用。

## 2\.3\.2 大量巡检照片处理与存储优化

难点描述：巡检员每完成一户巡检需上传1\-多张照片，单张照片原始体积较大，批量上传时易出现卡顿、失败，且大量照片存储会占用过多服务器资源，影响系统运行效率；同时需保证照片真实性，防止篡改。

解决方案：前端上传前对照片进行自动压缩处理，将照片分辨率压缩至合适尺寸（如1080P），保留核心细节的同时减小文件体积；采用分片上传机制，将大体积照片分割为多个小分片依次上传，失败后仅重传失败分片，提升上传成功率；照片存储采用MinIO/阿里云OSS对象存储，按"项目\-电站\-巡检时间"分级建立存储目录，便于检索和管理；上传时自动添加不可篡改的文本水印（巡检人、时间、经纬度），同时记录照片上传时间戳，确保照片可追溯、不可篡改。

## 2\.3\.3 超大数据量异步导出性能优化

难点描述：当巡检计划包含数百、数千户电站时，批量导出PDF巡检报告和照片会产生大量数据处理任务，同步导出会阻塞系统主线程，导致系统响应缓慢，甚至出现导出失败、服务器卡顿等问题。

解决方案：采用Quartz定时任务框架结合异步线程池，实现导出任务异步执行，管理员提交导出请求后，系统后台启动独立线程处理导出任务，不占用系统主线程；采用流式ZIP打包方式，打包过程中不将文件落地服务器，直接通过流的方式生成ZIP文件，减少服务器存储空间占用；设置导出任务优先级，避免多个大额导出任务同时执行，占用过多系统资源；导出完成后，通过系统通知告知管理员，同时提供导出文件短期缓存（如7天），便于管理员下载，过期自动清理，释放存储资源。

## 2\.3\.4 多模块数据实时同步一致性

难点描述：系统中电站状态、项目统计、计划统计、巡检记录等数据分布在多个模块，巡检员提交巡检记录后，需同步更新电站巡检状态、项目已巡检数、计划完成率等数据，若同步机制不完善，易出现数据不一致（如电站已巡检但项目统计未更新），影响数据准确性。

解决方案：采用事务管理机制，将巡检记录提交与相关数据更新纳入同一事务，确保要么全部执行成功，要么全部回滚，避免数据部分更新导致的不一致；采用事件驱动模式，巡检记录提交后触发事件，同步通知电站模块、项目模块、计划模块更新对应统计数据；添加数据一致性校验定时任务，定期比对各模块关联数据，发现不一致时自动触发修正，同时记录校验日志，便于后续追溯。

## 2\.3\.6 权限控制精细化与安全性

难点描述：系统存在管理员和巡检员两种角色，权限划分需精准到接口级别，避免巡检员越权操作（如修改其他巡检员的巡检记录、创建巡检计划）；同时需防止非法请求、数据泄露等安全风险，保障系统和数据安全。

解决方案：基于Spring Security框架实现精细化权限控制，为不同角色分配专属权限列表，接口请求时进行权限拦截校验，仅允许具备对应权限的角色访问；用户密码采用BCrypt加密存储，同时添加密码复杂度校验（如长度、包含大小写字母和特殊符号），防止密码泄露；接口请求添加Token令牌验证，设置Token有效期，超时自动失效，同时拦截非法请求、重复请求；对敏感数据（如电站信息、巡检记录）进行加密处理，数据传输采用HTTPS协议，防止数据被窃取、篡改。

# 3\. 功能模块划分

基于《光伏巡检系统需求规格说明书》的要求，结合系统业务流程，将系统按功能划分为7个核心模块，各模块独立实现对应业务功能，通过标准化接口实现模块间的交互协同，确保模块间低耦合、高内聚，便于开发、测试及后续扩展，具体模块划分及详细说明如下：

## 3\.1 权限与用户模块

### 3\.1\.1 模块职责

权限与用户模块是系统安全访问的核心模块，负责用户身份验证、权限分配、账号管理及个人中心维护，实现对系统所有用户的统一管理和权限控制，拦截非法访问请求，保障系统访问安全，同时为其他模块提供用户身份信息支撑。

### 3\.1\.2 核心功能

- 登录鉴权：实现用户用户名与密码的校验，支持登录超时自动退出机制，设置登录会话有效期，同时实现首次登录强制修改初始密码的功能，确保账号安全，避免初始密码泄露导致的安全风险。

- 用户管理：仅管理员具备该功能权限，实现系统用户的新增、编辑、删除、查询操作，支持用户账号的启用与禁用，能够对忘记密码的用户进行密码重置，同时记录用户账号的操作日志，便于后续追溯。

- 角色控制：采用基于角色的权限控制（RBAC）模式，为系统用户分配管理员和巡检员两种角色，明确不同角色的权限范围，管理员拥有系统所有操作权限，巡检员仅拥有巡检作业相关的操作权限，实现接口级别的权限拦截。

- 个人中心：所有用户均可访问，支持用户个人密码修改、个人基本信息（如联系电话）查看与编辑，同时收纳关于系统、版本信息、退出登录等次要功能，为用户提供便捷的个人信息管理入口。

## 3\.2 项目管理模块

### 3\.2\.1 模块职责

项目管理模块负责光伏巡检项目的全生命周期管理，作为电站信息的归属主体，实现项目基础信息的维护、电站与项目的关联管理，为电站管理模块、巡检计划模块提供项目数据支撑，严格贴合"电站归属项目"的核心业务约束，确保巡检工作按项目有序开展。

### 3\.2\.2 核心功能

- 项目基础操作：支持项目的新增、编辑、删除、查询操作，新增项目时需录入项目名称、产权公司、电站类型、所在地区、资质证书等基础信息，编辑操作仅允许修改非关键信息，删除项目时需先删除项目关联的电站及巡检记录，避免数据冗余和关联异常。

- 项目统计：实时统计每个项目下的电站总数、已完成巡检的电站数、未巡检的电站数及巡检完成率，统计数据实时同步更新，便于管理员直观了解各项目的巡检进度，为巡检工作调度提供依据。

- 电站关联：支持在项目内批量导入电站信息（通过Excel文件）、批量删除电站信息，实现电站与项目的关联与解除关联操作，关联电站时需校验电站编号的唯一性，避免重复关联，删除电站时同步删除其关联的巡检记录及巡检照片。

- 项目搜索：提供项目名称模糊搜索功能，支持管理员和巡检员快速定位目标项目，点击项目即可进入该项目下的电站列表，支撑巡检员快速开展巡检作业，提升操作效率。

## 3\.3 电站管理模块

### 3\.3\.1 模块职责

电站管理模块隶属于项目管理模块，无独立入口，需在具体项目下进行操作，负责项目内电站基础信息的维护、搜索筛选及状态同步，为巡检作业模块提供电站基础数据支撑，确保巡检填报时能够快速关联电站信息，保障巡检工作的精准性。

### 3\.3\.2 核心功能

- 电站信息管理：支持电站信息的新增、编辑、查询操作，维护电站的核心基础信息，包括电站编号、户主姓名、发电户号、逆变器序列号、逆变器品牌型号、组件规格型号、组件块数、装机容量、装机地址等，确保电站信息的准确性和完整性，编辑电站信息时需同步更新关联的巡检记录中的电站信息。

- 电站搜索筛选：提供多条件搜索筛选功能，支持按户主姓名、电站编号进行模糊搜索，按巡检状态（未巡检/已巡检）进行筛选，便于巡检员快速找到需要巡检的电站，提升巡检作业效率。

- 电站批量操作：支持通过Excel文件批量导入电站信息，导入时进行数据校验，确保电站编号唯一、必填字段完整；支持批量删除电站信息，删除时同步删除该电站关联的所有巡检记录、巡检照片及相关统计数据，避免数据残留。

- 电站状态同步：根据巡检作业模块提交的巡检记录，实时更新电站的巡检状态，当电站完成巡检并提交记录后，自动将电站状态从"未巡检"切换为"已巡检"，同时更新项目统计和计划统计中的相关数据，确保数据一致性。

## 3\.4 巡检计划模块

### 3\.4\.1 模块职责

巡检计划模块仅对管理员开放操作权限，负责巡检计划的创建、编辑、结束及状态管理，明确巡检任务的执行范围（通过多对多关联多个项目）、执行时间，实现巡检计划的全生命周期管理，同时统计各计划的巡检进度，约束巡检任务的有序执行。

### 3\.4\.2 核心功能

- 计划创建：支持巡检计划的创建，通过inspect_plan_project关联表实现计划与项目的多对多关联，一个计划可批量应用至多个项目，便于管理员统一调度多个项目的巡检工作。

- 计划管理：支持巡检计划的编辑和结束操作，编辑操作仅允许修改计划的起止时间（计划未开始或进行中状态下），结束计划后，该计划下的巡检任务停止执行，巡检记录变为只读状态，不允许修改；同时支持按项目筛选巡检计划，便于管理员快速查看特定项目的计划情况。

- 状态管理：系统自动根据计划的起止时间判定计划状态，分为未开始、进行中、已结束三种状态；计划开始时间到达后自动切换为"进行中"，结束时间到达后自动切换为"已结束"，同时支持管理员手动结束计划，应对特殊场景下的计划终止需求。

- 计划统计：实时统计每个巡检计划下的电站总数、已巡检数、未巡检数及巡检完成率，支持按项目展示各关联项目的完成率排行，便于管理员直观了解各计划、各项目的巡检进度，及时调度巡检资源。

- 计划约束：设置计划创建约束规则，同一项目在同一时间仅允许存在一个进行中的巡检计划，禁止重复创建，避免巡检任务冲突，确保巡检工作有序开展。

## 3\.5 巡检作业模块

### 3\.5\.1 模块职责

巡检作业模块是巡检员的核心操作模块，负责巡检任务的填报、巡检记录的管理、巡检照片的处理及经纬度采集，实现巡检过程的全流程支撑，简化巡检员的操作路径，确保巡检数据的真实、准确、完整，为后续统计分析和报告生成提供基础数据。

### 3\.5\.2 核心功能

- 巡检填报：巡检员进入巡检页面后，系统自动关联当前进行中的巡检计划、所在项目及电站信息，无需手动选择，巡检员仅需填写巡检内容（按6大项77小项的结构化清单填报）、上传巡检照片、采集当前巡检位置经纬度，填写完成后提交巡检记录，操作流程简洁高效。

- 记录管理：支持巡检员提交巡检记录，提交后可在计划进行中状态下修改本人提交的巡检记录（仅允许修改巡检内容和照片），当巡检计划结束后，所有巡检记录变为只读状态，不允许任何修改，确保巡检数据的真实性和不可篡改。

- 照片处理：支持巡检照片的上传、预览、删除操作，上传时自动对照片进行压缩处理，减少存储空间占用和上传时间，同时为照片添加水印，水印内容包括巡检人姓名、巡检时间、巡检位置经纬度，确保照片的真实性和可追溯性，上传完成后将照片存储至对象存储中。

- 经纬度采集：支持自动获取巡检员当前位置的经纬度，精准定位巡检地点，同时支持手动微调经纬度（应对定位偏差场景），确保巡检位置真实可查，避免虚假巡检，为巡检工作的合规性提供保障。

- 状态联动：巡检员提交巡检记录后，系统自动联动更新相关数据，包括电站的巡检状态（未巡检→已巡检）、项目统计中的已巡检数和完成率、巡检计划统计中的已巡检数和完成率，确保所有相关数据实时同步，保持数据一致性。

## 3\.6 统计与导出模块

### 3\.6\.1 模块职责

统计与导出模块负责系统所有巡检数据的统计分析和数据导出，实现多维度、全方位的巡检数据统计，生成标准化的巡检报告，支持批量导出巡检报告和照片，满足管理员对巡检数据的查看、分析、归档及上报需求。

### 3\.6\.2 核心功能

- 多维度统计：支持全局、巡检计划、项目三个维度的巡检完成情况统计，全局维度统计系统所有项目、所有计划的总体巡检数据，计划维度统计单个巡检计划的巡检进度，项目维度统计单个项目的巡检进度，所有统计数据实时更新，以图表或列表形式直观展示，便于管理员快速掌握巡检工作整体情况。

- 报告生成：支持单个电站巡检报告的PDF生成和在线预览功能，报告内容包括电站基础信息、巡检计划信息、巡检时间、巡检人、巡检内容、巡检照片、经纬度等核心信息，报告模板标准化，确保报告的规范性和统一性。

- 批量导出：支持按单个巡检计划批量导出PDF巡检报告和巡检照片，采用流式ZIP打包方式，打包过程不落地服务器，避免服务器存储空间占用，导出的ZIP文件包含该计划下所有电站的巡检报告和对应照片，便于管理员进行数据归档和上报。

- 异步导出：针对超大数据量（如包含数百、数千电站的巡检计划）的导出需求，启用异步任务处理机制，管理员提交导出请求后，系统后台异步执行导出操作，导出完成后通过系统通知告知管理员，管理员可随时下载导出文件，不影响系统正常运行。

- 导出约束：设置导出约束规则，仅允许按单个巡检计划进行批量导出，禁止全量历史数据导出，避免导出操作占用过多系统资源，同时保障数据安全，防止大量巡检数据泄露。

## 3\.7 通用工具模块

### 3\.7\.1 模块职责

通用工具模块是系统的基础支撑模块，提供全局通用的工具类和服务，供其他所有模块调用，封装常用的功能逻辑，减少代码冗余，提升开发效率，确保系统各模块功能的统一性和规范性，降低开发和维护成本。

### 3\.7\.2 核心功能

- 图片处理：提供图片压缩、水印添加、格式转换等功能，压缩功能可根据需求调整压缩比例，平衡图片质量和文件大小；水印功能支持添加文本水印（如巡检人、时间、经纬度），确保图片可追溯；格式转换支持将不同格式的图片转换为统一格式，便于存储和展示。

- PDF处理：提供PDF生成、预览、压缩等功能，PDF生成支持模板填充，可根据巡检数据动态生成标准化巡检报告；PDF预览支持在线查看PDF文件，无需下载；PDF压缩可减少PDF文件体积，便于存储和导出。

- 异步任务：提供异步任务调度功能，处理批量导出、数据库定时备份、计划状态自动切换等耗时操作，将耗时操作异步执行，避免阻塞系统主线程，提升系统响应速度和运行效率。

- 加密解密：提供用户密码加密、数据传输加密等功能，用户密码采用BCrypt加密存储，确保密码安全；数据传输过程中采用加密算法，防止数据被窃取或篡改，保障数据传输安全。

- 参数校验：提供接口请求参数校验功能，对前端发送的请求参数进行合法性校验（如必填字段校验、格式校验、长度校验等），拦截非法请求，避免无效数据进入系统，保障系统数据安全和稳定运行。

- 对象存储操作：提供对象存储（MinIO/OSS）的统一操作接口，实现巡检照片、PDF报告等非结构化数据的上传、下载、删除、查询等操作，封装对象存储的底层细节，为其他模块提供便捷的调用接口。

# 4\. 数据库设计

数据库采用MySQL 8\.0，设计严格遵循数据库第三范式，合理划分数据表，减少数据冗余，建立完善的主键、外键关联，确保数据的一致性、完整性和安全性。系统核心数据表共11张，分别为：sys_user（用户表）、project（项目表）、station（电站表）、inspect_device（巡检设备表）、inspect_plan（巡检计划表）、inspect_plan_project（计划项目关联表）、inspect_record（巡检记录表）、inspect_section（巡检大项表）、inspect_section_item（巡检小项表）、export_task（导出任务表）、inspect_checklist_template（巡检模板表，已废弃，由inspect_section + inspect_section_item替代），覆盖系统所有业务场景，各数据表结构详细说明如下（明确字段名、字段类型、主键、默认值及备注信息）：

## 4\.1 用户表（sys_user）

用户表用于存储系统所有用户的基础信息，是实现用户登录、权限控制的核心数据表，关联巡检记录表，记录巡检员的巡检操作关联信息，确保巡检记录可追溯到具体操作人员。

| 字段名         | 字段类型       | 是否主键 | 默认值                                        | 备注                                                                                           |
| -------------- | -------------- | -------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| id             | bigint         | 是       | AUTO_INCREMENT                                | 用户唯一标识，作为用户表的主键，用于关联其他数据表                                            |
| username       | varchar\(30\)  | 否       | 无                                            | 用户登录账号，系统内唯一，用于用户登录验证，不可重复                                          |
| password       | varchar\(60\)  | 否       | 无                                            | 用户登录密码，采用BCrypt加密后存储，确保密码安全，不可明文存储                                |
| real_name      | varchar\(20\)  | 否       | 无                                            | 用户真实姓名，用于巡检记录、报告生成等场景，展示真实操作人员信息                              |
| phone          | varchar\(20\)  | 否       | 无                                            | 用户联系电话，可选填写，用于系统通知、紧急联系等场景                                          |
| role           | varchar\(30\)  | 否       | inspector                                     | 用户角色，仅支持两种取值：admin（管理员）、inspector（巡检员），用于权限控制                  |
| status         | tinyint        | 否       | 1                                             | 账号状态，1表示正常，0表示禁用，禁用后用户无法登录系统                                        |
| need_reset_pwd | tinyint        | 否       | 1                                             | 是否强制修改密码，1=是 0=否，首次登录或管理员重置密码后需强制修改密码                          |
| create_time    | datetime       | 否       | CURRENT_TIMESTAMP                             | 用户账号创建时间，自动记录，无需手动填写                                                       |
| update_time    | datetime       | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 用户账号信息更新时间，当用户信息修改时自动更新                                                 |

## 4\.2 项目表（project）

项目表用于存储光伏巡检项目的基础信息，作为电站信息的归属主体，关联电站表和巡检计划表，是实现项目管理、电站关联、巡检计划分配的核心数据表，确保巡检工作按项目有序开展。

| 字段名                   | 字段类型        | 是否主键 | 默认值                                        | 备注                                                                 |
| ------------------------ | --------------- | -------- | --------------------------------------------- | -------------------------------------------------------------------- |
| id                       | bigint          | 是       | AUTO_INCREMENT                                | 项目唯一标识，作为项目表的主键，用于关联电站表、巡检计划表           |
| project_name             | varchar\(100\)  | 否       | 无                                            | 项目名称，系统内唯一，用于项目识别和搜索，不可重复                   |
| property_company         | varchar\(100\)  | 否       | 无                                            | 项目产权公司名称，用于记录项目归属主体，便于管理和追溯               |
| station_type             | varchar\(50\)   | 否       | 无                                            | 电站类型，如分布式光伏电站、集中式光伏电站等，用于区分项目类型       |
| province                 | varchar\(10\)   | 否       | 无                                            | 项目所在省份                                                         |
| city                     | varchar\(20\)   | 否       | 无                                            | 项目所在城市                                                         |
| drone_certificate_url    | varchar\(255\)  | 否       | 无                                            | 民用无人机驾驶合格证图片地址                                         |
| special_operation_cert_url | varchar\(255\) | 否       | 无                                            | 特种作业操作证图片地址                                               |
| create_time              | datetime        | 否       | CURRENT_TIMESTAMP                             | 项目创建时间，自动记录，无需手动填写                                 |
| update_time              | datetime        | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 项目信息更新时间，当项目信息修改时自动更新                           |

## 4\.3 电站表（station）

电站表用于存储光伏巡检项目下的电站基础信息，隶属于具体项目，关联项目表和巡检记录表，是巡检作业填报的核心数据支撑，确保巡检工作能够精准关联到具体电站。

| 字段名                 | 字段类型         | 是否主键 | 默认值                                        | 备注                                                     |
| ---------------------- | ---------------- | -------- | --------------------------------------------- | -------------------------------------------------------- |
| id                     | bigint           | 是       | AUTO_INCREMENT                                | 电站唯一标识                                             |
| project_id             | bigint           | 否       | 无                                            | 项目ID，关联project.id，索引idx_project_id               |
| station_code           | varchar\(50\)    | 否       | 无                                            | 电站编号，系统内唯一                                     |
| owner_name             | varchar\(30\)    | 否       | 无                                            | 户主姓名，索引idx_owner_name                             |
| address                | varchar\(255\)   | 否       | 无                                            | 装机地址                                                 |
| power_account          | varchar\(50\)    | 否       | 无                                            | 发电户号                                                 |
| inverter_sn            | varchar\(50\)    | 否       | 无                                            | 逆变器序列号                                             |
| inverter_brand         | varchar\(50\)    | 否       | 无                                            | 逆变器品牌型号                                           |
| module_spec            | varchar\(100\)   | 否       | 无                                            | 组件规格型号                                             |
| module_count           | int              | 否       | 无                                            | 组件块数                                                 |
| capacity_kw            | decimal\(10,2\)  | 否       | 无                                            | 装机容量kW                                               |
| longitude              | decimal\(12,8\)  | 否       | 无                                            | 经度                                                     |
| latitude               | decimal\(12,8\)  | 否       | 无                                            | 纬度                                                     |
| status                 | tinyint          | 否       | 0                                             | 巡检状态：0=未巡检 1=已巡检，索引idx_status              |
| last_inspect_record_id | bigint           | 否       | 无                                            | 最后巡检记录ID                                           |
| last_inspect_time      | datetime         | 否       | 无                                            | 最后巡检时间                                             |
| create_time            | datetime         | 否       | CURRENT_TIMESTAMP                             | 创建时间                                                 |
| update_time            | datetime         | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间                                                 |

## 4\.4 巡检计划表（inspect_plan）

巡检计划表用于存储巡检计划的基础信息，通过inspect_plan_project关联表实现与项目的多对多关系，支持单项目或多项目的巡检任务调度。

| 字段名          | 字段类型       | 是否主键 | 默认值                                        | 备注                                                  |
| --------------- | -------------- | -------- | --------------------------------------------- | ----------------------------------------------------- |
| id              | bigint         | 是       | AUTO_INCREMENT                                | 计划唯一标识                                          |
| plan_name       | varchar\(30\)  | 否       | 无                                            | 计划名称                                              |
| start_time      | date           | 否       | 无                                            | 开始日期                                              |
| end_time        | date           | 否       | 无                                            | 结束日期                                              |
| status          | tinyint        | 否       | 0                                             | 状态：0=未开始 1=进行中 2=已结束，索引idx_status      |
| creator_id      | bigint         | 否       | 无                                            | 创建人用户ID，索引idx_creator_id                      |
| total_count     | int            | 否       | 0                                             | 电站总数                                              |
| inspected_count | int            | 否       | 0                                             | 已巡检总数                                            |
| create_time     | datetime       | 否       | CURRENT_TIMESTAMP                             | 创建时间                                              |
| update_time     | datetime       | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间                                              |

## 4\.5 计划项目关联表（inspect_plan_project）

计划项目关联表用于实现巡检计划与项目的多对多关系，一个巡检计划可关联多个项目，记录每个关联项目的电站总数和已巡检数。

| 字段名          | 字段类型       | 是否主键 | 默认值                                        | 备注                                                  |
| --------------- | -------------- | -------- | --------------------------------------------- | ----------------------------------------------------- |
| id              | bigint         | 是       | AUTO_INCREMENT                                | 关联唯一标识                                          |
| plan_id         | bigint         | 否       | 无                                            | 关联计划ID                                            |
| project_id      | bigint         | 否       | 无                                            | 关联项目ID                                            |
| total_count     | int            | 否       | 0                                             | 该项目电站总数                                        |
| inspected_count | int            | 否       | 0                                             | 该项目已巡检数                                        |
| create_time     | datetime       | 否       | CURRENT_TIMESTAMP                             | 创建时间                                              |
| update_time     | datetime       | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间                                              |

> UNIQUE KEY uk_plan_project(plan_id, project_id)

## 4\.6 巡检记录表（inspect_record）

巡检记录表用于存储巡检员提交的巡检记录，关联巡检计划、项目、电站及巡检员信息，存储结构化的巡检结果和照片数据。

| 字段名           | 字段类型         | 是否主键 | 默认值                                        | 备注                                                  |
| ---------------- | ---------------- | -------- | --------------------------------------------- | ----------------------------------------------------- |
| id               | bigint           | 是       | AUTO_INCREMENT                                | 记录唯一标识                                          |
| plan_id          | bigint           | 否       | 无                                            | 关联计划ID，索引idx_plan_id                           |
| plan_project_id  | bigint           | 否       | 无                                            | 关联计划项目，索引idx_plan_station复合                |
| project_id       | bigint           | 否       | 无                                            | 关联项目ID，索引idx_project_id                        |
| station_id       | bigint           | 否       | 无                                            | 关联电站ID，索引idx_station_id                        |
| inspector_id     | bigint           | 否       | 无                                            | 巡检人员，索引idx_inspector_id                        |
| weather          | varchar\(50\)    | 否       | 无                                            | 天气情况                                              |
| checklist_result | json             | 否       | 无                                            | 巡检结果JSON                                          |
| photos           | json             | 否       | 无                                            | 照片URL按section分组JSON                              |
| longitude        | decimal\(12,6\)  | 否       | 无                                            | 经度                                                  |
| latitude         | decimal\(12,6\)  | 否       | 无                                            | 纬度                                                  |
| pdf_url          | varchar\(255\)   | 否       | 无                                            | 报告PDF地址                                           |
| create_time      | datetime         | 否       | CURRENT_TIMESTAMP                             | 创建时间                                              |
| update_time      | datetime         | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间                                              |

## 4\.7 巡检大项表（inspect_section）

巡检大项表用于存储巡检清单的6大分类项，每个大项包含多个巡检小项，替代原有的inspect_checklist_template表。

| 字段名      | 字段类型            | 是否主键 | 默认值                                        | 备注             |
| ----------- | ------------------- | -------- | --------------------------------------------- | ---------------- |
| id          | bigint              | 是       | AUTO_INCREMENT                                | 大项唯一标识     |
| section_no  | tinyint unsigned    | 否       | 无                                            | 大项序号1-6，UNIQUE |
| section_name | varchar\(30\)      | 否       | 无                                            | 大项名称         |
| create_time | datetime            | 否       | CURRENT_TIMESTAMP                             | 创建时间         |
| update_time | datetime            | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间         |

> 初始数据：1=光伏组件，2=支架，3=逆变器，4=配电箱，5=接地与防雷系统，6=采集装置及电缆

## 4\.8 巡检小项表（inspect_section_item）

巡检小项表用于存储每个巡检大项下的具体巡检条目，共77个条目，定义巡检内容、类别及填报类型。

| 字段名      | 字段类型            | 是否主键 | 默认值                                        | 备注                                                       |
| ----------- | ------------------- | -------- | --------------------------------------------- | ---------------------------------------------------------- |
| id          | bigint              | 是       | AUTO_INCREMENT                                | 小项唯一标识                                               |
| section_id  | bigint              | 否       | 无                                            | 所属大项ID                                                 |
| category    | varchar\(20\)       | 否       | 无                                            | 类别                                                       |
| item_no     | tinyint unsigned    | 否       | 无                                            | 小项序号                                                   |
| content     | varchar\(500\)      | 否       | 无                                            | 巡检内容                                                   |
| item_type   | tinyint             | 否       | 1                                             | 填报类型：1=正常/异常 2=实测值 3=照片                      |
| create_time | datetime            | 否       | CURRENT_TIMESTAMP                             | 创建时间                                                   |
| update_time | datetime            | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间                                                   |

> UNIQUE KEY uk_section_item(section_id, item_no)。共77个条目。

## 4\.9 巡检设备表（inspect_device）

巡检设备表用于存储项目关联的巡检设备信息，记录设备名称和型号。

| 字段名      | 字段类型       | 是否主键 | 默认值                                        | 备注                            |
| ----------- | -------------- | -------- | --------------------------------------------- | ------------------------------- |
| id          | bigint         | 是       | AUTO_INCREMENT                                | 设备唯一标识                    |
| project_id  | bigint         | 否       | 无                                            | 关联项目ID，索引idx_project_id  |
| device_name | varchar\(50\)  | 否       | 无                                            | 设备名称                        |
| device_model | varchar\(50\) | 否       | 无                                            | 设备型号                        |
| create_time | datetime       | 否       | CURRENT_TIMESTAMP                             | 创建时间                        |
| update_time | datetime       | 否       | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间                        |

## 4\.10 导出任务表（export_task）

导出任务表用于存储异步导出任务的记录，支持PDF报告ZIP和照片ZIP两种导出类型，记录任务状态、进度及下载信息。

| 字段名          | 字段类型       | 是否主键 | 默认值            | 备注                                                   |
| --------------- | -------------- | -------- | ----------------- | ------------------------------------------------------ |
| id              | bigint         | 是       | AUTO_INCREMENT    | 任务唯一标识                                           |
| plan_id         | bigint         | 否       | 无                | 关联计划ID，索引idx_plan_id                            |
| operator_id     | bigint         | 否       | 无                | 操作人用户ID，索引idx_operator_id                      |
| status          | tinyint        | 否       | 0                 | 任务状态：0=处理中 1=已完成 2=失败，索引idx_status     |
| file_url        | varchar\(500\) | 否       | 无                | 下载链接                                               |
| file_size       | bigint         | 否       | 无                | 文件大小（bytes）                                      |
| total_count     | int            | 否       | 0                 | 导出总份数                                             |
| export_type     | tinyint        | 否       | 0                 | 导出类型：0=PDF报告ZIP 1=照片ZIP                       |
| processed_count | int            | 否       | 0                 | 已处理数                                               |
| error_message   | varchar\(500\) | 否       | 无                | 失败原因                                               |
| create_time     | datetime       | 否       | CURRENT_TIMESTAMP | 创建时间                                               |
| complete_time   | datetime       | 否       | 无                | 完成时间                                               |

## 4\.11 巡检模板表（inspect_checklist_template）（已废弃）

巡检模板表为系统早期版本使用的巡检清单模板存储表，现已废弃，巡检清单结构由inspect_section（巡检大项表）和inspect_section_item（巡检小项表）两张表替代，提供更灵活的模板管理能力。

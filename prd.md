# 光伏巡检系统需求规格说明书

# 1\. 文档概述

## 1\.1 编写目的

本文档明确光伏巡检系统的功能需求、业务规则、页面交互、权限约束及非功能需求，统一系统开发、测试、验收标准，确保系统贴合管理员、巡检员的实际操作场景，满足光伏电站巡检的全流程管理需求。

## 1\.2 适用范围

本需求文档适用于光伏巡检系统的开发人员、测试人员、产品人员及使用系统的两类角色（管理员、巡检员），覆盖项目管理、电站管理、巡检计划、巡检填报、数据统计与导出等全业务流程。

## 1\.3 核心设计原则

- 电站必须归属项目，无独立电站管理入口，所有电站操作均遵循“项目→电站”路径；

- 移动端优先，PC端响应式兼容，界面简洁不拥挤，适配巡检员工地单手操作场景；

- 简化巡检员操作路径，减少层级跳转，提升日常巡检效率；

- 权限严格隔离，数据按角色展示，保障数据安全与合规性；

- 避免冗余功能与页面，聚焦核心业务，提升系统易用性。

## 1\.4 运行环境

- 前端：移动端（手机端优先）、PC端响应式兼容；

- 后端：Java服务部署；

- 数据库：MySQL 8\.0；

- 文件存储：对象存储（OSS/MinIO，用于存储巡检照片、PDF报告）。

# 2\. 角色与权限

## 2\.1 角色定义

### 2\.1\.1 管理员

负责系统全局配置、用户管理、项目管理、电站管理、巡检计划管理、数据统计与导出，可查看系统内所有数据，拥有全部操作权限。

### 2\.1\.2 巡检员

负责查看所有项目、进入项目电站列表、对电站执行巡检填报、上传巡检照片、查看本人提交的巡检记录，无任何管理权限，不绑定具体项目，可跨项目查看电站并执行巡检。

## 2\.2 核心权限约束

- 管理员、巡检员均无独立“电站管理”入口，电站所有操作均隶属于项目模块；

- 巡检员仅可查看本人提交的巡检记录，不可查看其他巡检员的记录，不可修改他人记录；

- 管理员可重置所有用户密码，巡检员仅可修改本人密码；

- 巡检计划仅管理员可创建、编辑、结束，巡检员仅可查看当前进行中的巡检计划；

- 电站必须归属具体项目，不可脱离项目独立存在，巡检员无法跳过项目直接查看电站。

# 3\. 功能需求

## 3\.1 登录与个人中心

### 3\.1\.1 登录功能

- 采用用户名\+密码登录模式，支持登录鉴权与权限拦截，未登录用户强制跳转至登录页；

- 新用户首次登录时，强制要求修改初始密码，密码修改完成后才可正常使用系统；

- 登录超时自动退出系统，保障账号安全；

- 支持密码重置：管理员可重置所有用户密码，巡检员仅可修改本人密码。

### 3\.1\.2 个人中心

- 展示用户基本信息（用户名、真实姓名、联系电话、角色）；

- 提供密码修改入口，支持原密码校验；

- 次要功能（关于系统、使用说明、退出登录）统一收纳至个人中心，不占用底部菜单资源。

## 3\.2 用户管理（仅管理员）

- 用户增删改查：支持新增用户、编辑用户信息、删除用户、启用/禁用用户账号；

- 角色分配：为用户分配“管理员”或“巡检员”角色，角色分配后即时生效；

- 用户筛选搜索：支持按用户名、真实姓名、角色、账号状态筛选搜索用户，快速定位目标用户；

- 密码重置：管理员可重置任意用户的密码，重置后用户需用初始密码登录并强制修改。

## 3\.3 项目管理

### 3\.3\.1 管理员功能

- 项目基础操作：支持项目新增、编辑、删除，录入项目名称、产权公司、电站类型等基础信息；

- 项目统计查看：查看每个项目的电站总数、已巡检数、未巡检数、巡检完成率；

- 电站关联管理：在项目内批量导入、删除电站，导出项目下电站巡检相关数据；

- 项目搜索：支持按项目名称搜索，快速定位目标项目。

### 3\.3\.2 巡检员功能

- 项目列表查看：展示所有项目，每个项目卡片显示项目名称、电站总数、已巡检数、完成率；

- 核心交互：点击任意项目，直接进入该项目的电站列表页（跳过项目详情页，缩短操作路径）；

- 项目信息查看：在电站列表页右上角提供“项目信息”入口，点击可查看该项目的完整信息及当前进行中的巡检计划。

## 3\.4 电站管理（隶属于项目，无独立入口）

### 3\.4\.1 电站列表页（独立页面）

电站列表为独立页面，仅在进入对应项目后可访问，页面结构简洁，聚焦搜索、筛选与巡检操作：

- 顶部区域：显示当前项目名称、简单统计（电站总数、已巡检数、未巡检数）；

- 搜索功能：顶部搜索框，支持按户主姓名、电站编号搜索，实时匹配结果；

- 筛选功能：筛选栏支持“全部电站”“未巡检”“已巡检”三种筛选条件，快速定位目标电站；

- 列表展示：采用滚动加载模式，支持大量电站（上千条）流畅展示，每行显示电站编号、姓名、当前巡检状态；

- 行内操作：每个电站行右侧直接设置“巡检”按钮（巡检员可见），点击可直接进入巡检填报页；支持点击电站行进入电站详情页。

### 3\.4\.2 管理员额外功能

- 电站批量操作：在电站列表页支持批量导入（Excel）、批量删除该项目下的电站，删除电站时同步删除其所有巡检记录及照片；

- 电站详情查看：查看电站完整信息（编号、姓名、发电户号、逆变器序列号、所属项目、最后巡检时间、最后巡检人）及所有历史巡检记录；

- 数据导出：导出该项目下电站的巡检报告、巡检照片（打包导出）。

### 3\.4\.3 巡检员额外功能

- 电站查看：仅可查看当前项目下的电站信息，支持按筛选条件快速找到未巡检电站；

- 巡检操作：点击电站行“巡检”按钮，直接进入巡检填报页，无需跳转电站详情页；

- 记录查看：进入电站详情页，可查看本人提交的该电站历史巡检记录，不可查看其他巡检员的记录。

## 3\.5 巡检计划管理（仅管理员）

- 计划创建：支持创建单项目巡检计划、全局统一巡检计划（可一键批量应用至多个项目或单个项目）；批量应用时，系统自动为每个选中项目生成独立子计划（同名称、同时间，数据独立）；

- 计划设置：可自定义巡检计划名称、起止时间（支持跨季度、跨月份），适配不同巡检周期需求；

- 计划状态：分为“未开始”“进行中”“已结束”三种，系统根据计划起止时间自动判定“进行中”状态，管理员可手动结束任意计划，未开始计划可修改起止时间；

- 计划约束：同一个项目，同一时间仅允许存在一个进行中巡检计划，已有进行中计划时，禁止新建新计划，系统给出明确提示；

- 计划统计：计划列表展示计划名称、所属项目（或涉及项目数）、起止时间、状态、电站总数、已巡检数、未巡检数、完成率；计划详情页展示全局汇总统计及各项目完成率排行；

- 计划操作：支持编辑计划（仅未开始/进行中计划可修改起止时间）、结束计划、按项目筛选计划；全局计划的子计划可单独编辑、结束，不影响其他项目的子计划；

- 数据导出：支持导出本次巡检计划下所有电站的巡检报告、巡检照片（打包导出），遵循批量导出规范，避免文件过大。

## 3\.6 巡检填报（巡检员核心功能）

### 3\.6\.1 操作路径（最简路径）

项目列表 → 对应项目电站列表 → 点击电站行“巡检”按钮 → 巡检填报页 → 提交记录

### 3\.6\.2 填报功能

- 自动关联信息：表单自动带出当前项目名称、户主姓名、电站编号、当前进行中的巡检计划，不可编辑，避免选择错误；

- 巡检内容：采用多行文本框，支持按预设模板填写巡检情况描述，满足不同巡检场景需求；

- 照片上传：支持多张照片上传，上传前自动压缩（压缩至500KB以内/宽度1920px），自动添加水印（巡检人、巡检时间、经纬度），确保照片可溯源、防篡改；

- 经纬度采集：通过浏览器自动获取当前位置经纬度，支持手动微调，确保巡检位置真实可查；

- 提交与修改：计划处于“进行中”状态时，可提交、修改本人提交的巡检记录；计划结束后，“提交”“修改”按钮禁用，记录变为只读；

- 提交后联动：提交成功后，自动返回电站列表页，同步更新该电站的巡检状态（未巡检→已巡检）、项目统计数据、巡检计划统计数据。

## 3\.7 巡检记录查看

- 查看权限：管理员可查看所有巡检记录；巡检员仅可查看本人提交的巡检记录，无项目限制；

- 展示内容：每条记录展示巡检内容、巡检照片（可预览）、经纬度信息、巡检时间、巡检人、所属巡检计划名称、所属项目名称，信息完整可追溯；

- 查看入口：管理员可通过项目电站列表→电站详情页查看所有记录；巡检员可通过项目电站列表→电站详情页查看本人记录。

## 3\.8 数据统计与导出

### 3\.8\.1 统计功能

- 统计维度：支持三层统计，全局维度（所有项目巡检总体完成情况）、巡检计划维度（某一批次巡检任务跨项目汇总统计）、项目维度（单个项目内部巡检完成情况），统计口径清晰，互不干扰；

- 统计展示：管理员首页展示全局统计卡片、进行中计划列表、项目完成率排行；项目电站列表页展示该项目简单统计；巡检计划详情页展示计划相关统计。

### 3\.8\.2 导出功能

- 单站导出：支持单个电站巡检报告（PDF格式）在线预览、单文件导出，修改巡检记录后，预览/导出自动生成最新版本；

- 批量导出：采用流式ZIP打包导出，逐一生成PDF/照片并写入压缩流，边生成边返回浏览器，不落地服务器、不占用大量内存，避免内存溢出；

- 导出约束：禁止导出项目全量历史巡检报告/照片，仅支持按单个巡检计划导出；超大数据量（如超过2000份）自动启用异步任务，生成完成后通过站内通知提醒管理员下载；

- 导出入口：管理员可通过巡检计划详情页导出该计划所有数据，可通过项目电站列表页导出该项目当前计划数据。

# 4\. 页面交互规范

## 4\.1 移动端菜单设计

采用顶部栏+侧边菜单栏设计

## 4\.2 核心页面交互流程

### 4\.2\.1 巡检员交互流程（最简路径）

登录 → 底部“首页”（项目列表） → 点击目标项目 → 项目电站列表（搜索/筛选电站） → 点击电站行“巡检” → 填写巡检记录 → 提交 → 返回电站列表

### 4\.2\.2 管理员交互流程

登录 → 底部“数据概览” → 底部“项目管理” → 选择目标项目 → 项目电站列表（管理电站/查看统计）；或通过“数据概览”进入巡检计划管理、用户管理。

## 4\.3 关键页面设计要求

- 项目列表页：卡片式展示，每个卡片仅显示核心信息（项目名称、电站总数、完成率），点击直接进入电站列表，不跳转项目详情页；

- 项目电站列表页：顶部搜索框\+筛选栏固定，列表滚动加载，电站行操作简洁，仅保留“巡检”按钮（巡检员），避免冗余；右上角“项目信息”入口隐藏次要信息；

- 巡检填报页：表单简洁，自动带出关联信息，照片上传区域清晰，提交按钮突出，适配手机单手操作；

- 个人中心页：列表式展示次要功能，分类清晰，便于查找。

## 4\.4 交互约束

- 无进行中巡检计划时，电站列表页“巡检”按钮置灰，提示“当前无可用巡检计划”；

- 项目下无电站时，电站列表页显示“暂无电站，请联系管理员添加”，无搜索/筛选功能；

- 计划结束后，巡检填报页、巡检记录修改入口均禁用，仅可查看；

- 移动端页面适配单手操作，按钮尺寸适中，避免误触；搜索框支持输入法联想，提升搜索效率。

# 5\. 业务约束规则

- 电站约束：电站必须归属具体项目，不可脱离项目独立存在，删除项目时需先删除该项目下所有电站；

- 巡检计划约束：同一项目同一时间仅允许一个进行中巡检计划，禁止重复创建；全局计划批量应用后，子计划独立存在，可单独操作；

- 巡检记录约束：仅计划处于“进行中”状态时，巡检员可修改本人提交的记录；计划结束后，记录只读，不可修改、不可删除；

- 导出约束：禁止导出项目全量历史巡检数据，仅支持按单个巡检计划导出，跨周期导出需限制时间范围（最大跨度不超过1年）；

- 数据隔离约束：项目间数据完全隔离，电站、巡检记录按项目关联；巡检员可查看所有项目电站，但仅可修改本人提交的记录。

# 6\. 非功能需求

## 6\.1 性能需求

- 加载速度：移动端页面首次加载≤3秒，后续页面跳转≤1秒；

- 数据承载：电站列表支持滚动加载，可流畅展示上千条电站数据，无卡顿；

- 导出性能：批量导出≤1000份巡检报告/照片时，同步导出完成；超过1000份时，异步导出，生成时间≤30分钟；

- 并发性能：支持≥50名巡检员同时在线巡检、上传照片，系统无崩溃、无数据丢失。

## 6\.2 安全需求

- 权限安全：基于角色的权限控制，无权限页面/接口不可访问，接口请求需进行权限校验；

- 数据安全：用户密码采用BCrypt加密存储，巡检记录不可篡改（计划结束后），照片加水印防篡改；

- 访问安全：登录超时自动退出，防止账号被盗用；禁止未登录用户访问系统任何页面；

- 文件安全：巡检照片、PDF报告存储在对象存储，支持访问权限控制，防止非法下载。

## 6\.3 易用性需求

- 操作简洁：巡检员核心操作路径不超过3步，避免复杂跳转；

- 提示清晰：操作错误、无权限、无数据时，给出明确提示，引导用户正确操作；

- 适配性：移动端适配不同手机型号、屏幕尺寸，PC端响应式适配，两端操作逻辑一致；

- 学习成本：界面简洁直观，巡检员、管理员无需专业培训，即可快速上手。

## 6\.4 可靠性需求

- 稳定性：系统7×24小时稳定运行，无频繁崩溃、卡顿现象；

- 数据可靠性：巡检记录、电站信息、项目数据实时同步，无数据丢失、数据错乱；

- 容错性：用户输入错误、操作失误时，系统不崩溃，给出友好提示并允许修正；

- 备份需求：数据库定期备份，防止数据丢失；对象存储文件支持备份，避免照片、PDF丢失。

# 7\. 附则

## 7\.1 文档更新

本需求文档将根据系统开发过程中的需求变更、业务调整，及时更新完善，确保与实际开发、使用场景一致。

## 7\.2 文档解释权

本需求文档的最终解释权归系统开发团队所有，如有疑问，可结合实际业务场景协商调整。

> （注：文档部分内容可能由 AI 生成）
